# Server-side base URL of the API. Never exposed to the browser.
API_URL=http://localhost:3001
# Sentry project DSN, baked into the browser bundle at build time. Leave unset to run
# without error reporting.
# NEXT_PUBLIC_SENTRY_DSN=https://public-key@o0.ingest.sentry.io/0
