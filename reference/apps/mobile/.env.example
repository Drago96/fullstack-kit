# Base URL of the API. Expo inlines EXPO_PUBLIC_* into the bundle at build time, so on a
# device this must be an address the device can reach, not localhost.
EXPO_PUBLIC_API_URL=http://localhost:3001
